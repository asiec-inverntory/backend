 type User {
   id: ID!
 }

 type Query {
    user(id: ID!):User
#     QR
#    encode(code: String): String
#    decode(image: String): String
 }

 type Mutation {
     createUser(name: String!):User
 }